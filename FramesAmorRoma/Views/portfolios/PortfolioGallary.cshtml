@model IEnumerable<FramesAmorRoma.Models.portfolio>

@{
    ViewBag.Title = "PortfolioGallary";
}

<h2>PortfolioGallary</h2>





<div class="row justify-content-center">

    @foreach (var item in Model)
    {

        @*<div class="row cols-12 mt-3">

                <div class="col-12 col-md-3 d-flex flex-column text-center text-uppercase    align-content-center">

                    <img src="~/Content/img/portfolio/@item.coverIMG" style="width:100px;height:100px; object-fit:cover; object-position:center" class=" ms-auto me-auto rounded-circle card my-2" alt="profile Picture">
                    <h5>
                        @Html.DisplayFor(modelItem => item.User.FirstName) @Html.DisplayFor(modelItem => item.User.LastName)
                    </h5>

                </div>
                <div class="col-12 col-md-9">
                    gallary
                </div>


            </div>*@

    <div class="card go2Gallary m-2 " style="max-width: 600px;">
        <div class="row g-0">
            <div class="col-md-4">
                <img src="~/Content/img/portfolio/@item.coverIMG" class="img-fluid rounded-start" style="height:100%; position:center; object-fit:cover" alt="...">
            </div>
            <div class="col-md-8">
                <div class="card-body">
                    <div class=" text-center ">
                        <img src="~/Content/img/profiles/@item.User.imgURL" class="img-fluid rounded-circle w-25 me-auto ms-auto" alt="...">
                        <h5 class="card-title">
                            @Html.DisplayFor(modelItem => item.User.FirstName) @Html.DisplayFor(modelItem => item.User.LastName)
                        </h5>
                    </div>

                </div>
            </div>
        </div>
        <div class="row mt-3">
            <p>@Html.DisplayFor(modelItem => item.User.aboutME)</p>
        </div>
        <div class="float-end" >
            @Html.ActionLink("Enter To Gallary", "Details", new { id = item.IDportfolio } ,new{ @class = "link-dark btn btn-outline-light my-0 fw-bolder border-0 rounded-start text-decoration-none " })
        </div>

    </div>
    }

</div>



